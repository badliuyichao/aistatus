# ── 运行期配置（含密钥，切勿提交）──
config.json
legacy/config.json

# ── Node / 前端 ──
node_modules/
dist/
.svelte-kit/
*.log
npm-debug.log*
.DS_Store

# ── Rust / Tauri ──
src-tauri/target/
src-tauri/gen/schemas/

# ── Python 旧版字节码（legacy 目录）──
__pycache__/
*.pyc

# ── 打包产物 ──
build/
*.dmg
*.app
